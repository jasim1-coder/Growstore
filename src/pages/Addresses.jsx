import React from "react";
import ProfileLayout from "../components/profile/ProfileLayout";

const Addresses = () => {
  return <ProfileLayout>Addresses</ProfileLayout>;
};

export default Addresses;
