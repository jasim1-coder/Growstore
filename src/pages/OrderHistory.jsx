import React from "react";
import ProfileLayout from "../components/profile/ProfileLayout";

const OrderHistory = () => {
  return <ProfileLayout>OrderHistory</ProfileLayout>;
};

export default OrderHistory;
